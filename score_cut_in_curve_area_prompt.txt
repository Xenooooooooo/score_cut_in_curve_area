现在，我想使用 coding agent 为我撰写一个 python 项目，实现如下的功能：

1. 运行python代码后，弹出选择文件夹界面。选择完成后，按顺序读取文件夹当中的图片，而后准备逐张处理图片。

2. 在读取图片及完成图片读取后，页面上展示交互界面，等待用户进行操作。界面上，正中间的画布展示图片，可对图片进行移动与缩放，同时支持用户使用鼠标在画布上圈定区域；上方存在“圈定区域”“撤销”“带边界区域裁剪”“下一张”四个按钮，其功能分别是：

【圈定区域】：用户在点击该按钮后，可使用鼠标在画布上进行圈定区域，第一次点击标记圈定线起点，而后鼠标在画布上进行移动，圈定曲线区域，直至曲线闭合后再次点击，即可完成一次圈定区域操作。完成一次圈定区域操作后，画布上使用虚线持续标记出用户所圈定的区域。

【撤销】：当画布上存在圈定区域时，用户可点击该按钮，使画布上所圈定的区域消失。若画布上本就没有圈定区域，则点击此按钮无作用。

【带边界区域裁剪】：当画布上存在圈定区域时，用户可点击该按钮，进行带边界区域裁剪。此按钮需要完成的工作如下：以左上角为 (0,0) 坐标点，以右为X正方向，以下为Y正方向，建立坐标系。考察圈定区域，首先得出圈定区域最上、最下、最左、最右的四个【非白色点】的坐标，并计算 最左【非白色点】到图片左边界的距离 和 最右【非白色点】到图片右边界的距离，取二者的较小值，记为 min。而后，以最上、最下、最左、最右的四个【非白色点】为上下左右的边界，裁剪出一块矩形区域，并在该矩形区域的基础上，向该区域上下左右四个方向向外添加宽度为 min 的白边，形成新的图片。输出该图片。若画布上本就没有圈定区域，则点击此按钮无作用。

【下一张】：用户在点击该按钮后，程序读取下一张图片，并等待用户进行操作。

3. 点击【圈定区域】的操作可使用 q 键进行快捷键代替，点击【带边界区域裁剪】的操作可使用 e 键进行快捷键代替，点击【撤销】的操作可使用 ctrl+z 键进行快捷键代替，点击【下一张】的操作可使用 右箭头（→） 键进行快捷键代替。

4. 处理完第一张图片后，在当前文件夹下生成一个名为 out 的文件夹，用于存放处理完成的图片，往后所有处理的图片均放入该文件夹，输出图片使用jpg格式。若输入图片问其他格式图片，在完成裁剪后仍输出jpg格式图片。

5. 同一张图片可以在完成一次【圈定区域】-【带边界区域裁剪】后进行再次处理，用户进行【撤销】后即可消除前一次的圈定区域，而后圈定新的区域，并进行裁剪工作；

6. 输出图片的文件名视在同一张图片上进行裁剪的次数而定，示例：若名为“atime.jpg”裁剪三次，则三次输出的图片名为“atime_1.jpg”“atime_2.jpg”“atime_3.jpg”。

请为我完成一个python项目，满足如上的图片裁剪功能。


“
我刚刚对你的代码进行了运行测试，使用该文件夹下test_pic当中的图片进行测试。在完成文件夹选择后，界面打开正常，但是下方的画布并未显示图片。因此也无法进行后续操作。请修复此bug
”

“
这次图片可以正常显示了，但是出现了两个新问题：
1. 图片的拖动、缩放等操作过于卡顿，响应慢；
2. 在开始圈定区域后，我单击【圈定区域】，并在画面上单击一次标定起点。而后，界面直接显示“已撤销圈定区域”，没有给我划定曲线区域 和 第二次单击的机会。
请修复以上bug
”

“
很好，工作正常！但是还有一个小问题需要调整：
1. 图片的拖动很丝滑了，但是缩放操作还是过于卡顿，响应慢。
请进行改进
”
